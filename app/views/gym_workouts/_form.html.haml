= form_for @gym_workout do |f|
  -if @gym_workout.errors.any?
    #error_explanation
      %h2= "#{pluralize(@gym_workout.errors.count, "error")} prohibited this gym_workout from being saved:"
      %ul
        - @gym_workout.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :workout
    = f.text_area :workout
  .actions
    = f.submit 'Save'
